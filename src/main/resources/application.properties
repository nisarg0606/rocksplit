spring.application.name=rocksplit
server.port=8080

# # PostgreSQL Configuration
# spring.datasource.url=jdbc:postgresql://localhost:5432/rocksplit
# spring.datasource.username=postgres
# spring.datasource.password=SplitAI_2025
# spring.datasource.driver-class-name=org.postgresql.Driver

# # Connection pool settings
# #spring.datasource.hikari.connection-timeout=20000
# #spring.datasource.hikari.maximum-pool-size=5
# #spring.datasource.hikari.minimum-idle=1
# #spring.datasource.hikari.idle-timeout=300000

# # JPA/Hibernate
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.show-sql=true

# # Flyway Migration
# #spring.flyway.enabled=false
# #spring.flyway.baseline-on-migrate=true

# # File Upload Settings for Receipt Images
# #spring.servlet.multipart.max-file-size=10MB
# #spring.servlet.multipart.max-request-size=10MB

# # Logging
# #logging.level.org.springframework.web=INFO
# #logging.level.org.hibernate=INFO
# #logging.level.com.rocksplit=DEBUG

# ## JWT Configuration
# #app.jwt.secret=rockSplit2025
# #app.jwt.expiration=86400000


# PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/rocksplit
spring.datasource.username=postgres
spring.datasource.password=root
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true

# Redis Caching (for scaling)
# spring.cache.type=redis
# spring.redis.host=localhost
# spring.redis.port=6379

# Security & JWT
# app.jwt.secret=rockSplit2025SecretKeyForSecurityWithAtLeast256Bits
# app.jwt.expiration=86400000

# Logging
logging.level.org.springframework.web=INFO
logging.level.org.hibernate=INFO
logging.level.com.rocksplit=DEBUG